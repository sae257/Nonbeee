<div class="container">
 <div class="row">
<div class="col-md-2 offset-md-8">

  <h5>投稿一覧</h5>
    <table class="table table-striped">
      <tr>
       <th>ジャンル名</th>
     </tr>
   <% @genres.each do |f| %>
      <tr>
        <td><%= link_to f.name, genre_path(f.id)%></td>
      </tr>
　 <% end %>
    </table>
  </div>
 </div>
</div>

